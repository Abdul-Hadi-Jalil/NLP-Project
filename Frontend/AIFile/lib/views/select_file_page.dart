import 'package:flutter/material.dart';

class SelectFilePage extends StatelessWidget {
  const SelectFilePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.blue,
      child: const Center(
        child: Text('Select File Page'),
      ),
    );
  }
}
